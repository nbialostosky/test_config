version: 2.1


parameters:
  p:
    type: integer
    default: 2

jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "build"
  pretest:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "pretest"
      - run: 
          command: |
           echo << pipeline.parameters.p >> = 4
           
      - run: 
          command: |
            echo "export PARALLELISM=4" >> $BASH_ENV   
           
  test:        
    docker:
      - image: circleci/node    
    parallelism: << pipeline.parameters.p >>
    steps:
      - checkout
      - run: echo "test"

      - run: echo $PARALLELISM
      - run: echo << pipeline.git.branch >>
#       - run:  
#           command: |
#             if [ << pipeline.git.branch >> = master ]; then
#                echo << parameters.p >> = 2
#             else  
#               echo << parameters.p >> = 4
#             fi
#       - run:  
#           command: |
#             if [ << pipeline.parameters.action >> = build ]; then
#                echo << parameters.p >> = 2
#             else  
#               echo << parameters.p >> = 4
#             fi
      - run : echo << pipeline.parameters.p >>      
workflows:
  first_workflow:
#     when:
#       equal: [ build, << pipeline.parameters.action >> ]
    jobs:
      - build:
          filters:
            branches:
              ignore :
                - master
#       - pretest        
      - test:
          requires:
            - build
  second_workflow:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
      - pretest
      - test:
          parameters.p: 4
          requires:
            - build
            
            
            #added a comment
