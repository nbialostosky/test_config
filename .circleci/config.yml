version: 2.1
parameters:
  rollback:
    type: string
    default: "latest"    
jobs:
  build:
    docker: 
      - image: circleci/node
    steps:
      - run: echo "hello world" 
      
workflows:
  test_workflow:
    jobs:
      - build
      
