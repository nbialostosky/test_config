version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:8.11.3-browsers
    steps:
      - run: echo "test"
      - run: echo "PR; ${CIRCLE_PR_NUMBER}"
      - run: echo "dang"
      - run: echo "PR-2; ${CIRCLE_PULL_REQUEST}"

workflows:
  build-test-deploy:
    jobs:
      - build
#
