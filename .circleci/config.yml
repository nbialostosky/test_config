jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "build"
  pretest:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "pretest"
  test:
    parameters:
      p:
        type: integer
        default: 1
    docker:
      - image: circleci/node    
    parallelism: << parameters.p >>
    steps:
      - checkout
      - run: echo "test"
      - run : echo << parameters.p >>
workflows:
  version: 2
  first_workflow:
    jobs:
      - build:
          filters:
            branches:
              ignore:
                - master
      - test:
          requires:
            - build
  second_workflow:
    jobs:
      - build:
          filters:
            branches:
              only: master
      - pretest
      - test:
          p: 3
          requires:
            - build
            
            
            
# version: 2.1
# jobs:
#   build:
#     parameters:
#       p:
#         type: integer
#         default: 1
#     parallelism: << parameters.p >>
#     machine: true
#     steps:
#       - checkout
# workflows:
#   workflow:
#     jobs:
#       - build:
#           p: 2


       
